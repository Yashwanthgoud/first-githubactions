name: First-flow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print greetings
        run: echo " Hello Iam from git actions"
      - name: print success
        run: echo " success message"
